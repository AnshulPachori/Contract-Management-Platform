# Contract-Management-Platform
A modern frontend Contract Management Platform designed to build reusable contract templates, generate contracts, manage dynamic fields, and enforce controlled contract lifecycles using clean UI and centralized state management.
